{
  "name": "modern-library-bibliography-tools-site",
  "version": "0.0.1",
  "description": "Static site for Medern Library Bibliography",
  "main": "index.js",
  "repository": "https://github.com/uvalib/modern-library-bibliography-tools.git",
  "author": "Doug Chestnut <dougchestnut@gmail.com>",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "CETEIcean": "^1.5.0"
  },
  "scripts": {
    "build": "yarn run build:local && yarn run build:docs",
    "build:local": "rimraf build && yarn run build:js && npx @11ty/eleventy",
    "build:docs": "rimraf docs && yarn run build:js && npx @11ty/eleventy --pathprefix='/modern-library-bibliography-tools' --output=docs",
    "build:js": "rollup -c",
    "serve": "concurrently npm:serve:*",
    "serve:js": "rollup -wc",
    "serve:eleventy": "npx @11ty/eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.0",
    "@rollup/plugin-node-resolve": "^13.1.3",
    "clean-css": "^5.2.2",
    "concurrently": "^7.0.0",
    "rimraf": "^3.0.2",
    "rollup": "^2.64.0"
  }
}
