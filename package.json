{
  "name": "@uvalib/monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test --stream",
    "publish-all": "lerna publish",
    "start": "serve docs",
    "lerna-fix": "lerna exec -- node lerna-fix.js"
  },
  "devDependencies": {
    "husky": "^6.0.0",
    "jest-webpack": "^0.5.1",
    "lerna": "^4.0.0",
    "lint-staged": "^11.0.0"
  }
}
