# `@uvalib/prettier-config`

> A [Prettier](https://prettier.io) config for the [Library](https://www.library.virginia.edu).

## Usage

**Install**:

```bash
$ yarn add --dev @uvalib/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@uvalib/prettier-config"
}
```
