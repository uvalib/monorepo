---
title: "Learning Hub"
---
    
<style>
   .learning__header {
       padding-bottom: .3em;
        border-bottom: 2px solid var(--uvalib-grey);
    }
    .learning__header i {
        margin-right: .5em;
    }
    .learning__container {
        display: flex;
        flex-direction: row;
        flex-wrap: wrap;
        justify-content: flex-start;
        align-content: flex-start;
        align-items: flex-start;
        margin-left: 3em;
    }
    .learning__container div {
        min-width: 150px;
        max-width: 210px;
        margin-top: 2em;
        margin-right: 2em;
    }
</style>

<p>Let us help you succeed! Learn to use Virgo, discover databases & subject guides, explore Information Literacy training, download lesson plan templates, and much more.</p>
<h2>Learn how to…<h2>
{% for tag in learningLevel1 %}
    <h2 class="learning__header"><i aria-hidden="true" class="{{ tag.iconCode }}"></i>{{ tag.name }}</h2>
    <div class="learning__container">
        {% for subtag in learningLevel2 %}{% if subtag.parent == tag.uuid %}
            <div>
                <h3 class="h5"><a href="/services/learning/{{ subtag.name | slug }}">{{ subtag.name }}</a></h3>
                <p>{{ subtag.description | safe }}</p>
            </div>
        {% endif %}{% endfor %}
    </div>
{% endfor %}
