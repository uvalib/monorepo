import { html } from 'lit';
import { fixture, expect } from '@open-wc/testing';
import { SitePageSearch } from '../src/SitePageSearch.js';
import '../src/site-page-search.js';

describe('SitePageSearch', () => {

/*
  it('has a default header "Hey there" and counter 5', async () => {
    const el = await fixture<SitePageSearch>(html`<site-page-search></site-page-search>`);

    expect(el.header).to.equal('Hey there');
    expect(el.counter).to.equal(5);
  });

  it('increases the counter on button click', async () => {
    const el = await fixture<SitePageSearch>(html`<site-page-search></site-page-search>`);
    el.shadowRoot!.querySelector('button')!.click();

    expect(el.counter).to.equal(6);
  });

  it('can override the header via attribute', async () => {
    const el = await fixture<SitePageSearch>(html`<site-page-search header="attribute header"></site-page-search>`);

    expect(el.header).to.equal('attribute header');
  });

  it('passes the a11y audit', async () => {
    const el = await fixture<SitePageSearch>(html`<site-page-search></site-page-search>`);

    await expect(el).shadowDom.to.be.accessible();
  });
  */
});
